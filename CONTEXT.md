# STM32G474 电机控制平台

电池供电的电机控制设备平台（M1 状态），正演进为具备物联网能力的成熟嵌入式产品。本词表记录该领域内已经定论的术语，供代码、文档与对话共用。

## Language

**模块 (module)**:
职责边界明确的组件：一个目录 + 公开头文件 + 可独立编译的单测。不是功能点。
_Avoid_: 功能块、子系统

**成熟 (mature)**:
产品状态的判据：可验证（构建+单测+硬件闭环）、可演进（分层+ADR 记录取舍）、可运维（日志/诊断/OTA）、可量产（产线工具与文档）。
_Avoid_: 完善、稳定（作为口号使用时）

**物联网能力 (IoT capability)**:
设备具备网络连接能力，并能被远程管理、升级与诊断。
_Avoid_: 上云、联网（过宽泛）

**连接抽象层 (connectivity layer)**:
位于业务与具体通道驱动之间的统一接口，使 WiFi / BLE / 蜂窝 / 有线等通道可插拔替换。
_Avoid_: 通信层、网络层（有歧义）

**能量预算 (energy budget)**:
单次充电周期内设备可消耗的总能量；一切功能（连接频率、数据处理、日志量、OTA 时机）必须在此预算内立项。电池供电的第一等约束。目标已定：待机 ≤20µA，续航按月计，联网每天 ≤1 次。
_Avoid_: 省电模式（只是手段之一）

**能量档位 (energy profile)**:
每个连接通道或功能在唤醒→工作→休眠周期内的能量代价模型；能量预算管理以此为依据决定连接时机与频率。连接抽象层的每个通道都必须携带自己的能量档位。
_Avoid_: 功耗（无周期概念，无法做调度决策）

**运行日志 (runtime log)**:
易失、面向开发的日志流（core/log 现状，UART 输出）；仅 DEBUG 构建启用，Release 编译期裁剪。不承诺持久性，不是诊断数据源。
_Avoid_: 日志（与事件账本混为一谈）

**事件账本 (event journal)**:
持久化、掉电可恢复、带 UTC 时间戳的关键事件/故障记录（落 SPI Flash / NVM）；远程诊断与故障对账的唯一数据源。存储策略：环形覆盖，最旧丢弃；记录带魔数+CRC+序列号，启动扫描恢复，容忍撕裂写。
_Avoid_: 日志、log 文件

**主动上报 (active report)**:
设备按能量档位定时发起的数据上报（默认每天 1 次，频率由 power 模块调度）；与被动服务（被连接）并列的设备行为模式。上报触发分两类：定时触发（上报调度）与事件触发（紧急事件可预算外上报）。
_Avoid_: 心跳（只报存活不报数据）

**协议适配器 (protocol adapter)**:
自包含的协议模块：组帧编解码 + 会话交互 + 数据映射，三者完全随协议走，不共享。经协议框架注册，可插拔替换。协议层第一原则：**协议一定会换，组帧与交互方式不保证一致**。
_Avoid_: 统一帧协议、协议核心（把参考协议的骨架当普适真理）

**协议框架 (protocol framework)**:
薄层：协议模块注册表、字节路由（按通道-协议绑定表）、生命周期与事件分发。不定义任何帧格式或交互方式。
_Avoid_: 协议栈（暗示固定协议栈）

**设备模型 (device model)**:
协议无关、**驱动无关**的产品数据面：状态/配置/历史/事件的稳定访问接口。业务代码与协议适配器只通过它读写数据点；数据来源（ADC/UART/计算）对访问者不可见，外设驱动不在设备模型内。
_Avoid_: 寄存器表、驱动封装、设备树

**设备 (device)**:
指整个产品（终端设备），不是某个外设。参考协议中「终端设备」即产品本身。
_Avoid_: 外设、模组（用于指产品时）

**外设驱动 (peripheral driver)**:
bsp/ 层对具体外设的封装（uart_/spi_flash_/led_/iwdg_ 等）：寄存器与字节级操作，不持有业务语义。设备模型最多暴露「关于外设的数据点」（链路状态、错误计数），不暴露外设本身。
_Avoid_: 设备驱动、设备模型（用于指驱动时）

**传输适配器 (transport adapter)**:
通道机制实现：字节收发 + 模组驱动流程（boot/attach/socket/休眠状态机）+ 细粒度链路状态与原因码上报 + 控制接口；不解析协议语义，**不持有业务策略**（策略归通信管理）。异常时停在 FAULT 状态等指令，不自主重试。
_Avoid_: 协议驱动（把通道和协议混为一谈）、模组管理（策略在业务层）

**通信管理 (comm management)**:
业务层通信策略所有者：订阅适配器的细粒度链路状态与原因码，决定模组上电/断电、复位、重试、休眠与能量档位（受能量预算约束）；通信事件写入事件账本。
_Avoid_: 模组驱动（机制与策略混淆）

**数据对象 (data object)**:
参考协议（NB-IoT 智能终端协议，见 docs/ 收录原文）的语义单位，DID 寻址：状态/记录/设置/数据集四类；A000H~AFFFH 为厂商自定义域。仅对该协议成立，不承诺通用。
_Avoid_: 把它当成平台通用概念

**事件记录 (event record)**:
协议层事件条目格式：事件码(2B)+BCD 时间(6B)+详情(1B)；事件账本为其供数，主站经读记录功能码按类型/时间段查询。
_Avoid_: 告警（只是事件的一种）

**历史记录 (history record)**:
周期性采样的工况数据（小时/日/月累积量），与事件账本并列的持久数据；主站按日期范围查询，掉电期间需按上电后数据补全。
_Avoid_: 日志（与事件账本混为一谈）

**上报调度 (upload schedule)**:
主站可配置的上报周期（按天/按月 + 时刻，如协议 2006H 数据对象）；power 模块唤醒调度表的数据源，落 config 持久化。
_Avoid_: 上传频率（无配置语义）

**安全边界 (safety boundary)**:
安全关键路径（电机保护、欠压、过温、堵转、硬件刹车）与业务/IoT 路径之间的隔离线；IoT 故障不得影响安全路径。未来 SIL 认证的预留边界。
_Avoid_: 安全模块（只是实现之一）
