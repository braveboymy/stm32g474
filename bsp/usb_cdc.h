#ifndef USB_CDC_H
#define USB_CDC_H

/* ============================================================================
 * USB CDC 虚拟串口（兼容接口头）
 * 实现已迁移至 bsp/usb_device/usbd_cdc_if.c（参考 ai_stm32_prj 标准实现），
 * 本头保留原 API 供业务层使用。
 * ==========================================================================*/

#include "usbd_cdc_if.h"

#endif /* USB_CDC_H */
