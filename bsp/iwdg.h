#ifndef IWDG_H
#define IWDG_H

/* ============================================================================
 * 独立看门狗（IWDG，LSI 时钟）
 * 超时配置见 board.h（BOARD_IWDG_*），喂狗周期必须小于超时时间
 * 用途：fail-fast 停机（fault_freeze）后的兜底复位；任务饿死/调度卡死检测
 * ==========================================================================*/

/* 初始化并启动看门狗（启动后不可关闭，只能复位） */
void iwdg_init(void);

/* 喂狗（业务周期任务调用） */
void iwdg_feed(void);

#endif /* IWDG_H */
